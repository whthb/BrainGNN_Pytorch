# HCP 任务态 BrainGNN 的 LR/RL 配对一致性研究报告

执行日期：2026-06-13

## 摘要

本研究关注 HCP 任务态 fMRI 中 LR/RL 相位编码方向对任务解码稳定性的影响，并在
BrainGNN 强基线上加入同一受试者、同一任务 LR/RL 真配对的预测一致性约束。研究
动机来自两个观察：第一，原始连接特征可以以约 `99.6%` 的 balanced accuracy
识别 LR/RL，说明图中包含很强的采集方向信息；第二，强基线对同一受试者、同一
任务 LR/RL 配对的预测一致率仅为 `0.7418`，说明方向差异已经影响任务预测稳定性。

正式实验使用固定的受试者隔离五折划分和三个模型初始化种子，共比较 15 个匹配的
fold-init 结果。验证集选择的配对损失权重为 `lambda_pair=0.2`。与
`CE + unit + 0.1 GLC` 强基线相比，配对一致性 BrainGNN 将 balanced accuracy
由 `0.7938 ± 0.0556` 提高到 `0.8058 ± 0.0505`，Macro F1 由
`0.7978 ± 0.0547` 提高到 `0.8091 ± 0.0509`。LR/RL 配对预测一致率由
`0.7418 ± 0.0572` 提高到 `0.7506 ± 0.0582`，但该提升较小且未达到探索性统计
显著性。

因此，当前结果支持的结论是：**LR/RL 配对一致性训练能够作为有效正则化，提高
任务解码性能，并带来轻微、任务相关的方向一致性改善；现有证据不足以宣称模型
已经实现强 LR/RL 方向不变性。**

## 1. 研究动机

### 1.1 问题背景

理想的任务态 fMRI 解码模型应主要利用与认知任务相关的稳定脑连接模式，而不应
依赖相位编码方向等采集条件产生的伪特征。对于同一受试者、同一任务的 LR 和 RL
运行，模型的任务预测及关键 ROI 选择原则上应保持较高一致性。

然而，LR/RL 相位编码方向会引入方向相关的成像畸变和连接估计差异。如果模型利用
了这些差异，则可能出现以下问题：

- 普通混合方向划分中的性能被方向相关特征部分抬高；
- 同一任务在 LR 和 RL 运行上的预测不一致；
- 模型跨方向泛化能力下降；
- 可解释 ROI 选择受到采集方向干扰。

### 1.2 本地数据提供的前置证据

本地 HCP 数据及前置诊断呈现出明确的方向偏移信号：

- 使用展平的 `corr + pcorr` 特征和严格受试者级划分，逻辑回归识别 LR/RL 的
  balanced accuracy 约为 `99.6%`；
- 早期 BrainGNN 诊断中，同一受试者、同一任务的 LR/RL 配对预测一致率约为
  `78.0%`，约 `20.6%` 的配对只有一个方向预测正确；
- 强基线在正式固定划分实验中的配对预测一致率为 `0.7418 ± 0.0572`；
- LR 到 RL 和 RL 到 LR 的跨方向 BrainGNN balanced accuracy 分别只有
  `0.5978 ± 0.0522` 和 `0.5681 ± 0.0986`。

跨方向实验中的单方向训练数据量约为 mixed 协议的一半，因此不能把全部性能下降
归因于方向域偏移。但结合 LR/RL 几乎可被原始连接特征完全识别的结果，方向偏移
仍然构成了充分的研究动机。

### 1.3 研究问题

本研究主要回答以下问题：

1. 直接约束同一受试者、同一任务 LR/RL 配对的预测分布，能否提高任务解码性能？
2. 配对一致性训练能否提高离散预测一致率，并降低预测分布差异？
3. 一致性收益是否在不同任务和不同模型初始化下稳定？
4. 配对约束能否同时提高 ROI 选择一致性并减少方向可解码信息？

## 2. 数据与图构建

### 2.1 数据概况

| 项目 | 数量 |
|---|---:|
| 受试者 | 343 |
| run 级图 | 1,235 |
| 认知任务 | 7 |
| 皮层 ROI | 68 |
| LR 图 | 586 |
| RL 图 | 649 |
| 同一受试者、同一任务 LR/RL 配对 | 291 |
| 含完整配对的受试者 | 156 |

291 个完整配对对应 582 个图，占全部图的 `47.13%`。所有 run 至少包含 176 个
时间点。本地数据不包含年龄、性别或认知量表等表型变量。

### 2.2 配对任务分布

| 任务 | LR/RL 配对数 |
|---|---:|
| EMOTION | 68 |
| GAMBLING | 54 |
| SOCIAL | 46 |
| LANGUAGE | 42 |
| RELATIONAL | 40 |
| WM | 28 |
| MOTOR | 13 |

配对数存在明显任务不平衡，尤其是 MOTOR 只有 13 对。因此，正式训练对配对任务
使用逆平方根频数加权，避免高频任务主导一致性损失，同时避免完全逆频率权重过度
放大低频任务。

### 2.3 图表示

每个 run 构建一个 68 ROI 脑图。正式实验沿用当前强基线的图构建设置：

- 边来源：partial correlation；
- 边筛选：正边 top 10%；
- 主模型：BrainGNN；
- 主任务：七类 HCP 认知任务分类。

## 3. 方法与实验设置

### 3.1 强基线

正式实验以已有消融结果中表现较好的配置作为强基线：

```text
L_baseline = L_task + L_unit + 0.1 * L_GLC
```

其中 `L_task` 为七分类交叉熵，`L_unit` 和 `L_GLC` 为 BrainGNN 的结构正则项。
TPK 损失被禁用，因为已有复现实验未显示其带来稳定性能收益。

### 3.2 前置方向对抗实验

在配对一致性主线之前，实验曾在最终图嵌入后加入 Gradient Reversal 方向分类头，
尝试删除全局方向信息：

```text
L = L_baseline + lambda_adv * L_direction_GRL
```

方向对抗模型的任务性能略低于强基线，配对一致率和方向探针改善也不稳定。这说明
在最终图嵌入上进行全局方向信息移除并非当前数据上的可靠方案。强基线最终嵌入的
方向线性探针 balanced accuracy 已接近随机水平，继续增强全局对抗约束还可能
同时删除任务判别信息。因此，后续主线改为直接约束真实 LR/RL 配对。

### 3.3 LR/RL 配对一致性 BrainGNN

配对模型在强基线上加入 LR/RL 预测分布的 Jensen-Shannon 散度：

```text
L = L_task + L_unit + 0.1 * L_GLC
    + lambda_pair * JS(p_LR, p_RL)
```

其中 `p_LR` 和 `p_RL` 是同一受试者、同一任务 LR/RL 图的七类预测概率。

实现遵循以下原则：

- 所有训练图均参与普通任务分类，未配对图不会被丢弃；
- 仅完整 LR/RL 配对计算 JS 一致性损失；
- 配对前向传播临时使用 evaluation 模式的 Dropout 和 BatchNorm，但保留梯度；
- 上述处理使 JS 主要度量方向差异，而不是随机 Dropout 噪声或 BatchNorm 更新；
- 每个 epoch 中，每个训练配对使用一次；
- 训练配对严格来自训练受试者，不使用验证或测试配对。

### 3.4 数据划分与随机种子

正式配对实验固定 `split_seed=123`，使用按“7 个任务 × 2 个方向”分层的
受试者隔离五折划分。训练、验证和测试受试者完全不重叠。

模型初始化种子为：

```text
123, 456, 789
```

固定划分、只改变初始化的设计能够更直接地评估训练随机性。正式结果包含
`5 folds × 3 initialization seeds = 15` 个匹配 fold-init 结果。

### 3.5 配对权重选择

候选配对损失权重为：

```text
lambda_pair ∈ {0.05, 0.1, 0.2, 0.5}
```

权重选择仅使用初始化种子 `123` 和 `456` 的验证集结果。选择规则为：

1. 候选模型的平均验证 balanced accuracy 相对基线下降不得超过 0.5 个百分点；
2. 在满足非劣约束的候选中，优先选择验证集 LR/RL 配对一致率最高的权重；
3. 若仍相同，则优先选择验证 balanced accuracy 更高的权重。

最终根据验证集选择 `lambda_pair=0.2`。测试集结果不参与权重选择。

### 3.6 检查点、训练和停止规则

为避免配对一致率提高但任务性能明显下降，基线和配对模型使用相同的多目标检查点
规则：

1. 保留验证 balanced accuracy 距离训练过程最优值不超过 0.5 个百分点的 epoch；
2. 在候选 epoch 中优先选择验证配对一致率更高者；
3. 再优先选择验证配对 JS 更低者。

其他训练设置如下：

- 最大训练 100 epoch；
- 至少训练 40 epoch；
- early-stopping patience 为 20 epoch；
- epoch 0--9 不施加配对损失；
- epoch 10--29 将配对损失权重线性增加到目标值；
- batch size 为 64；
- 配对任务平衡方式为 inverse-square-root frequency；
- 每折进行 1,000 次受试者级 bootstrap。

正式实验共完成 60 个 fold 训练任务。强基线平均训练 `73.8` epoch，其中
12/15 个训练运行提前停止；最终配对模型平均训练 `82.4` epoch，其中 13/15 个运行
提前停止。

### 3.7 评价指标与统计说明

任务解码指标：

- accuracy；
- balanced accuracy；
- Macro F1。

配对一致性指标：

- LR/RL 离散预测一致率；
- 各任务一致率的宏平均；
- 配对同时预测正确率；
- LR/RL 预测概率 JS；
- LR/RL 平均概率集成后的 accuracy 和 balanced accuracy。

表示与解释指标：

- 第一层 pooling 选择 ROI 的配对 Jaccard；
- 冻结图表示后的 LR/RL 方向线性探针 balanced accuracy。

除特别说明外，结果均为 15 个匹配 fold-init 结果的 `mean ± std`。配对 t 检验
仅用于探索性比较：不同初始化重复使用同一批测试受试者，交叉验证折之间也共享
部分训练受试者，因此 15 个结果并非相互独立；同时未进行多重比较校正。

## 4. 实验结果

### 4.1 前置方向鲁棒性实验

| 模型或协议 | Balanced accuracy | Macro F1 | 配对一致率 | 方向探针 BAcc |
|---|---:|---:|---:|---:|
| 方向实验强基线 | `0.7972 ± 0.0486` | `0.7997 ± 0.0477` | `0.7258 ± 0.0616` | `0.5884 ± 0.0413` |
| 方向对抗模型 | `0.7921 ± 0.0560` | `0.7954 ± 0.0572` | `0.7335 ± 0.0930` | `0.5764 ± 0.0326` |
| LR 到 RL | `0.5978 ± 0.0522` | `0.5892 ± 0.0496` | - | - |
| RL 到 LR | `0.5681 ± 0.0986` | `0.5530 ± 0.1107` | - | - |

方向对抗模型相对其基线的 balanced accuracy 差值为 `-0.0051`
（探索性 `p=0.51221`），配对一致率差值为 `+0.0077`
（探索性 `p=0.67112`）。结果未显示稳定收益，支持转向真实配对一致性约束。

该前置实验中随机种子同时控制数据划分和模型初始化，其协议与后续固定划分配对
实验不同，因此两张结果表中的绝对数值不应直接进行模型优劣比较。

### 4.2 配对损失权重筛选

| `lambda_pair` | 验证 BAcc | 验证配对一致率 | 测试 BAcc |
|---:|---:|---:|---:|
| 0.05 | `0.8556 ± 0.0209` | `0.7958 ± 0.0521` | `0.8397 ± 0.0191` |
| 0.10 | `0.8634 ± 0.0216` | `0.7999 ± 0.0355` | `0.8348 ± 0.0237` |
| 0.20 | `0.8664 ± 0.0231` | `0.8034 ± 0.0545` | `0.8371 ± 0.0250` |
| 0.50 | `0.8596 ± 0.0197` | `0.8018 ± 0.0441` | `0.8334 ± 0.0281` |

在满足验证 balanced accuracy 非劣约束的候选中，`lambda_pair=0.2` 具有最高的
验证配对一致率，因此被选为最终权重。表中的测试结果仅用于完整记录，不参与选择。

### 4.3 正式实验总体结果

| 指标 | 强基线 | 配对一致性模型 | 平均差值 | 探索性 `p` |
|---|---:|---:|---:|---:|
| Accuracy | `0.8143 ± 0.0523` | `0.8251 ± 0.0490` | `+0.0108` | - |
| Balanced accuracy | `0.7938 ± 0.0556` | `0.8058 ± 0.0505` | `+0.0119` | `0.03136` |
| Macro F1 | `0.7978 ± 0.0547` | `0.8091 ± 0.0509` | `+0.0114` | `0.03119` |
| 配对预测一致率 | `0.7418 ± 0.0572` | `0.7506 ± 0.0582` | `+0.0088` | `0.34127` |
| 任务宏平均配对一致率 | `0.7030 ± 0.0733` | `0.7159 ± 0.0813` | `+0.0129` | `0.35737` |
| 配对同时正确率 | `0.7186` | `0.7313` | `+0.0128` | `0.21628` |
| 配对预测 JS | `0.1241` | `0.1207` | `-0.0034` | `0.13203` |
| 配对集成 accuracy | `0.8915` | `0.8988` | `+0.0073` | `0.14223` |
| 配对集成 BAcc | `0.8692 ± 0.0754` | `0.8727 ± 0.0771` | `+0.0036` | `0.76179` |
| Pool1 ROI Jaccard | `0.6718` | `0.6759` | `+0.0041` | `0.08373` |
| 方向探针 BAcc | `0.6011` | `0.5968` | `-0.0043` | `0.63458` |

配对模型在 15 个匹配结果中的 13 个提高了 balanced accuracy，在 12 个提高了
Macro F1，说明任务性能收益对初始化具有较好稳定性。相比之下，配对预测一致率只
在 7/15 个匹配结果中提高，其总体增幅较小。

### 4.4 不同初始化下的稳定性

| 初始化种子 | 基线 BAcc | 配对模型 BAcc | 基线一致率 | 配对模型一致率 | 基线任务宏一致率 | 配对模型任务宏一致率 |
|---:|---:|---:|---:|---:|---:|---:|
| 123 | `0.8267` | `0.8417` | `0.7760` | `0.7971` | `0.7257` | `0.7719` |
| 456 | `0.8318` | `0.8326` | `0.7614` | `0.7610` | `0.7381` | `0.7319` |
| 789 | `0.7230` | `0.7430` | `0.6880` | `0.6939` | `0.6454` | `0.6440` |

三个初始化种子的平均 balanced accuracy 均提高，表明任务解码收益不是由单个
初始化驱动。但一致性收益主要来自种子 123，种子 456 几乎没有变化，种子 789
只有小幅改善，说明一致性效果对优化轨迹更敏感。

### 4.5 任务级配对结果

| 任务 | 基线一致率 | 配对模型一致率 | 一致率差值 | 同时正确率差值 | 集成 accuracy 差值 | JS 差值 |
|---|---:|---:|---:|---:|---:|---:|
| EMOTION | `0.7821` | `0.8078` | `+0.0257` | `+0.0228` | `+0.0006` | `-0.0088` |
| GAMBLING | `0.6352` | `0.6826` | `+0.0474` | `+0.0501` | `+0.0154` | `-0.0102` |
| LANGUAGE | `0.9241` | `0.9167` | `-0.0074` | `-0.0074` | `-0.0231` | `+0.0022` |
| MOTOR | `0.4056` | `0.4944` | `+0.0889` | `+0.0889` | `-0.0222` | `-0.0320` |
| RELATIONAL | `0.7244` | `0.6928` | `-0.0317` | `-0.0011` | `+0.0428` | `-0.0003` |
| SOCIAL | `0.8681` | `0.8620` | `-0.0061` | `-0.0083` | `-0.0074` | `+0.0020` |
| WM | `0.5819` | `0.5552` | `-0.0267` | `-0.0194` | `+0.0190` | `+0.0047` |

一致性改善主要集中在 MOTOR、GAMBLING 和 EMOTION，其中 GAMBLING 的一致率
提升在未校正的探索性配对检验中达到 `p=0.0465`。其他任务没有一致改善，说明
配对约束并非对所有任务统一有效。

RELATIONAL 的离散一致率下降，但配对概率集成 accuracy 提高 `0.0428`。这说明
“两个方向给出相同离散类别”和“两个方向联合后更准确”并不是同一目标；一致性
指标必须与任务正确率共同解释。

## 5. 总结与讨论

### 5.1 主要发现

第一，LR/RL 配对一致性损失稳定提高了任务解码性能。balanced accuracy 平均提高
约 1.19 个百分点，并在 13/15 个匹配 fold-init 结果中提高；三个初始化种子的
平均性能也均提高。因此，可将该损失视为对当前 BrainGNN 有效的配对正则化方法。

第二，一致性相关指标总体沿预期方向变化，但改善较小。配对一致率、任务宏平均
一致率、同时正确率和 ROI Jaccard 均提高，预测 JS 和方向探针 BAcc 均降低，但
这些变化没有显示出稳定的探索性统计证据。

第三，效果具有明显任务异质性。MOTOR、GAMBLING 和 EMOTION 的一致性提高，
而 RELATIONAL、WM、LANGUAGE 和 SOCIAL 没有改善。配对一致性更可能帮助基线
一致率较低或决策边界较不稳定的任务，而不是形成统一的方向不变表示。

### 5.2 为什么任务性能改善大于一致性改善

配对 JS 约束作用于预测概率分布。即使其强度不足以改变 LR/RL 两个方向的最终
离散类别，也可能平滑分类边界、减少过度置信并改善泛化。因此，模型可以在整体
任务 balanced accuracy 上获得稳定收益，但离散配对一致率只发生较小变化。

此外，只有 47.13% 的图属于完整 LR/RL 配对。任务交叉熵作用于全部训练图，而
配对 JS 只作用于其中的完整配对，因此一致性约束的覆盖范围有限。较小的
`lambda_pair=0.2`、warm-up 和非劣检查点规则也共同限制了模型为追求一致性而
牺牲任务性能的程度。

### 5.3 可支持与不可支持的结论

当前结果支持：

- 同一受试者、同一任务的 LR/RL 真配对可以用于改进 BrainGNN；
- 配对一致性训练在固定划分和多个初始化下提高任务解码性能；
- 一致性相关指标总体出现轻微、任务相关的改善；
- 相比最终嵌入方向对抗，直接配对约束是当前数据上更有效的模型主线。

当前结果不支持：

- 模型已经学习到强 LR/RL 方向不变表示；
- 配对一致率在所有任务上均得到改善；
- 方向相关信息已经被显著消除；
- 跨方向性能下降完全由方向域偏移造成；
- 探索性 `p` 值可被解释为独立重复下的确认性统计证据。

### 5.4 研究限制

- 数据仅来自本地 HCP 数据集，缺少外部数据集或站点验证；
- 只有 291 个完整 LR/RL 配对，且任务分布不均，MOTOR 仅有 13 对；
- 配对图仅覆盖全部图的 47.13%，限制了一致性监督范围；
- 正式实验只使用一个固定数据划分种子，主要评估的是初始化稳定性；
- 交叉验证折和重复初始化结果并非完全独立，统计检验仅为探索性；
- 跨方向协议的训练数据量少于 mixed 协议，存在数据量混杂；
- 当前损失只约束最终预测分布，没有直接约束中间表示或方向校正后的连接图；
- 缺少年龄、性别和行为表型，无法验证一致性训练对其他下游任务的影响。

### 5.5 推荐论文表述

推荐将该工作表述为：

> 本研究利用同一受试者、同一任务的 LR/RL 真配对，在 BrainGNN 中加入预测分布
> 一致性正则。固定受试者划分和多个模型初始化的实验表明，该方法稳定提高任务
> 解码性能，并带来轻微、任务相关的方向一致性改善。结果说明配对监督能够改善
> 模型泛化，但尚不足以形成完全方向不变的表示。

不建议将工作表述为“消除了相位编码方向偏差”或“显著提高了整体 LR/RL
一致性”。更准确的贡献定位是：**利用配对采集结构改进任务解码，并系统评估
方向一致性、任务异质性和解释稳定性。**

## 6. 结论

LR/RL 配对一致性是当前本地 HCP 数据上可行且有效的 BrainGNN 改进方向。最终
模型在不丢弃未配对图的条件下，将 balanced accuracy 提高约 1.19 个百分点，
并在多数匹配结果中优于强基线。其主要收益体现在任务解码，而非强方向不变性。

从论文贡献角度，该实验可以作为模型改进主结果；跨方向实验和方向对抗实验可作为
问题动机与方法选择依据；任务级一致性、预测 JS、概率集成和 ROI Jaccard 可作为
机制分析与补充实验。

## 7. 复现实验入口

完整正式实验：

```bash
conda run --no-capture-output -n braingnn_rtx5070ti \
  python -u 18-run_hcp_pair_consistency.py \
  --phase all --jobs 1
```

仅重新汇总已有结果：

```bash
conda run --no-capture-output -n braingnn_rtx5070ti \
  python -u 18-run_hcp_pair_consistency.py \
  --phase summarize
```

相关文件：

- 正式配对实验报告：`experiments/hcp900_pair_consistency_20260613/REPORT.md`
- 正式配对实验汇总：`experiments/hcp900_pair_consistency_20260613/summary.json`
- 正式实验协议：`experiments/hcp900_pair_consistency_20260613/protocol.json`
- 固定划分清单：`experiments/hcp900_pair_consistency_20260613/splits/split_seed_123.json`
- 方向鲁棒性报告：`experiments/hcp900_direction_robustness_20260612/REPORT.md`
- 配对实验入口：`18-run_hcp_pair_consistency.py`
- 单折训练入口：`07-main_hcp_subjectwise.py`
