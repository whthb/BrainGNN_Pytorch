# BrainGNN 本地 HCP 数据研究扩展可行性分析

## 总体判断

基于当前仓库、已有复现实验和本地 HCP 数据，最推荐的论文主线是：

> 面向相位编码方向偏移的鲁棒 BrainGNN：通过方向不变表示提高 HCP 任务态解码的泛化性与解释一致性。

这个方向可以同时覆盖以下三类工作：

- 改进模型；
- 使用 BrainGNN 完成新的跨域解码任务；
- 解释损失函数和图结构设计。

不建议优先继续扩大网络容量。现有实验中，约 96k 参数模型的 balanced
accuracy 为 `0.8269`，低于 55k 参数模型的 `0.8449`。当前最优配置实际是去掉
TPK 的 `CE + unit + GLC`，达到 `0.8601 +/- 0.0204`，高于论文式完整损失的
`0.8449 +/- 0.0205`。

现有完整结果参见：

`experiments/hcp900_subjectwise_paper_reproduction_current_20260612/REPORT.md`

## 本地数据条件

当前数据包含：

- 343 名受试者；
- 1,235 个 run 级图；
- 7 类认知任务；
- 68 个皮层 ROI；
- 586 个 LR 样本和 649 个 RL 样本；
- 291 对同一受试者、同一任务的 LR/RL 样本；
- 每个样本均保存完整 ROI 时间序列；
- 所有 run 至少包含 176 个时间点。

本地数据没有年龄、性别和认知量表等表型，因此目前不能可靠开展相关表型预测
任务。

额外诊断结果如下：

- 使用展平的 `corr + pcorr` 特征和严格受试者级划分，简单逻辑回归识别
  LR/RL 的 balanced accuracy 约为 `99.6%`；
- 现有 BrainGNN 在 LR 和 RL 上的 balanced accuracy 分别约为 `0.8548` 和
  `0.8396`；
- 同一受试者、同一任务的 LR/RL 配对预测一致率仅为 `78.0%`；
- 约 `20.6%` 的配对中只有一个方向预测正确。

这些结果说明连接图中存在非常强的采集方向信息，并且已经影响任务预测稳定性。

## 推荐主线：方向不变 BrainGNN

### 研究动机

理想的任务态解码模型应主要学习与认知任务相关的连接模式，而不是依赖 LR/RL
相位编码方向产生的采集特征。当前数据中 LR/RL 极易被识别，同时同一任务的
LR/RL 预测仍存在较大不一致，因此可以通过方向不变训练提高模型鲁棒性。

### 模型设计

在 BrainGNN 图级表示后增加一个 LR/RL 方向分类头，并通过 Gradient Reversal
Layer 进行对抗训练：

```text
L = L_task + L_unit + 0.1 * L_GLC + lambda_adv * L_direction_GRL
```

训练目标为：

- 主分类头正确识别七类认知任务；
- 图表示尽量无法区分 LR/RL；
- 降低模型对采集方向伪特征的依赖。

建议以当前最优的 `CE + unit + GLC` 作为基线，暂时去掉 TPK。现有结果已经
表明 TPK 没有带来性能收益。

可选增强是在 291 对 LR/RL 样本上加入预测一致性损失：

```text
L_pair = JS(p_LR, p_RL)
```

第一阶段只实现方向对抗头。如果结果有效，再增加配对一致性损失。

### 对比模型

1. 当前最优 BrainGNN：`CE + unit + GLC`；
2. BrainGNN + 方向对抗损失；
3. BrainGNN + LR/RL 配对一致性；
4. 方向对抗 + 配对一致性，可作为可选最终模型。

方向对抗权重使用验证集选择：

```text
lambda_adv = {0.01, 0.05, 0.1, 0.2}
```

### 核心指标

- 普通受试者级五折 balanced accuracy 和 macro F1；
- LR 与 RL 性能差距；
- LR/RL 配对预测一致率；
- 跨方向任务解码性能；
- 冻结图表示后的 LR/RL 线性探针准确率；
- 配对 ROI 选择 Jaccard；
- 跨折解释稳定性。

合理的成功标准是：

- 普通测试 balanced accuracy 下降不超过 1 个百分点；
- 跨方向 balanced accuracy 提高至少 2 个百分点；
- LR/RL 配对预测一致率提高至少 5 个百分点；
- 冻结表示后的方向分类准确率显著降低。

## 新任务：跨方向任务解码

这是最适合当前数据的新任务设置：

- 使用训练受试者的 LR 数据训练，测试新受试者的 RL 数据；
- 反向执行 RL 到 LR；
- 训练、验证和测试受试者严格不重叠。

现有简单逻辑回归的跨方向 balanced accuracy 为：

- LR 到 RL：约 `0.7886`；
- RL 到 LR：约 `0.8095`。

该实验能够直接回答：

> 模型识别的是稳定的认知任务连接模式，还是依赖特定采集方向的模式？

它比单纯报告普通随机划分准确率更有科学意义，也能自然支撑方向不变模型。

## 低风险新任务：短扫描任务解码

如果希望避免较大的模型改动，可以开展：

> 有限扫描长度下的 BrainGNN 任务解码与收缩协方差图构建。

所有任务至少有 176 个时间点，因此可以统一使用：

```text
40 / 80 / 120 / 160 / 完整时间点
```

对每种长度重新计算连接图，并比较：

1. 普通 Pearson correlation 和 partial correlation；
2. Ledoit-Wolf 收缩协方差及对应的 partial correlation。

短时间序列下，特别是 `40 < 68 ROI` 时，普通相关矩阵估计不稳定。收缩估计
可能改善分类性能和解释稳定性。该方向几乎不需要修改 BrainGNN，主要工作是增加
数据构建流程，实施风险较低。

## 建议补充的结构与参数实验

### 图构建敏感性

固定当前最优损失，比较：

```text
pcorr positive top 5%
pcorr positive top 10%
pcorr positive top 20%
corr positive top 10%
pcorr absolute-magnitude top 10%
```

该实验可以解释为什么选择 partial correlation 和 10% 稀疏度。

需要注意：当前 `--no-positive_edges_only` 使用的是连接绝对值，并不是真正保留
符号的负边模型。

### Pooling Ratio

比较以下 pooling ratio：

```text
ratio = {0.4, 0.5, 0.6, 0.7}
```

经过两层池化后大约保留：

```text
11 / 17 / 25 / 33 个 ROI
```

报告分类性能、ROI 稳定性和训练时间，可以解释原论文的 `ratio=0.5` 是否仍适合
当前较小的 68-ROI 图。

## 可行性与风险排序

| 方向 | 可行性 | 预期贡献 | 风险 |
|---|---|---|---|
| 方向不变 BrainGNN | 高 | 模型改进、跨域泛化、解释稳定性 | 需要实现对抗训练和配对评估 |
| 跨方向任务解码 | 很高 | 新任务、鲁棒性评价协议 | 单独作为贡献时模型创新较弱 |
| 短扫描与收缩连接 | 高 | 新任务、图构建方法分析 | 需要生成多套数据 |
| 图稀疏度敏感性 | 很高 | 参数选择解释 | 创新性有限，适合作为补充实验 |
| Pooling ratio 分析 | 很高 | 结构设计解释 | 创新性有限，适合作为补充实验 |
| 增大模型容量 | 低 | 潜在性能提升 | 已有结果不支持，容易过拟合 |
| 年龄、性别或认知预测 | 当前不可行 | 新任务 | 本地缺少表型标签 |

## 推荐执行顺序

1. 将 `CE + unit + GLC` 确定为新的强基线。
2. 完成普通划分与 LR 到 RL、RL 到 LR 跨方向基线。
3. 实现方向对抗 BrainGNN，并进行方向损失权重选择。
4. 对基线与最终模型执行 `5 folds x 3 seeds`。
5. 补充一个图稀疏度或 pooling ratio 实验。
6. 如时间允许，再加入短扫描与收缩连接实验。

当前每折 100 epoch 训练约需 3.3 分钟，因此计算成本不高，主要工作量在模型
修改、实验组织和结果分析。

## 方法成功时的预期论文叙事

如果方向不变训练通过多随机种子验证，论文可以形成以下叙事：

> 现有 BrainGNN 能够有效解码 HCP 认知任务，但功能连接表示包含强烈的相位编码
> 方向信息，并导致同一任务跨方向预测不一致。本文提出方向不变 BrainGNN，通过
> 对抗训练抑制方向相关伪特征，在保持任务分类性能的同时，提高跨方向泛化、
> 配对预测一致性和可解释 ROI 选择稳定性。

后续完整实验表明，当前简单方向对抗实现没有稳定达到上述目标，因此现阶段不能
使用这一结论作为论文叙事。

## 推荐顺序 1-4 的执行结果

执行日期：2026-06-12。

完整协议、逐折结果和自动报告位于：

`experiments/hcp900_direction_robustness_20260612/`

### 已完成内容

1. 将 `CE + unit + 0.1 GLC`、禁用 TPK 的配置固化为强基线。
2. 完成 mixed、LR 到 RL 和 RL 到 LR 三种方向协议。
3. 实现 Gradient Reversal 方向对抗头，并在
   `lambda_adv = {0.01, 0.05, 0.1, 0.2}` 中使用验证集选择权重。
4. 对强基线和最终方向对抗模型完成 `5 folds x 3 seeds` 实验。

所有划分均按 14 个“任务 x 方向”组合进行分层，并保持训练、验证和测试受试者
完全隔离。三个随机种子同时控制受试者划分和模型初始化。

### 权重选择

种子 123 的五折平均验证 balanced accuracy 为：

| `lambda_adv` | 验证 balanced accuracy |
|---:|---:|
| 0.01 | `0.8544 +/- 0.0204` |
| 0.05 | `0.8555 +/- 0.0321` |
| 0.1 | `0.8655 +/- 0.0331` |
| 0.2 | `0.8619 +/- 0.0265` |

因此最终选择 `lambda_adv = 0.1`。

### 三种子主结果

| 模型 | Balanced accuracy | Macro F1 | LR/RL 配对预测一致率 | 方向线性探针 balanced accuracy |
|---|---:|---:|---:|---:|
| 强基线 | `0.7972 +/- 0.0486` | `0.7997 +/- 0.0477` | `0.7258 +/- 0.0616` | `0.5884 +/- 0.0413` |
| 方向对抗 BrainGNN | `0.7921 +/- 0.0560` | `0.7954 +/- 0.0572` | `0.7335 +/- 0.0930` | `0.5764 +/- 0.0326` |

15 个匹配 seed-fold 对的配对比较结果为：

- balanced accuracy 差值：`-0.0051`，配对 t 检验 `p=0.51221`；
- macro F1 差值：`-0.0043`，`p=0.60871`；
- LR/RL 配对预测一致率差值：`+0.0077`，`p=0.67112`；
- 方向线性探针 balanced accuracy 差值：`-0.0120`，`p=0.32225`；
- LR/RL balanced accuracy 绝对差距由 `0.0391` 降至 `0.0320`，
  差值 `-0.0071`，`p=0.47598`。

由于交叉验证折之间共享部分训练受试者，15 个 seed-fold 结果并非完全独立，
上述配对 t 检验仅作为探索性统计，不应视为严格的独立重复检验。

方向对抗在种子 123 上改善明显，但在其他种子上不稳定。综合三种子结果，当前
模型没有达到预定义成功标准，不能宣称方向对抗带来了可靠提升。

### 跨方向任务解码结果

| 协议 | Balanced accuracy | Macro F1 |
|---|---:|---:|
| LR 到 RL | `0.5978 +/- 0.0522` | `0.5892 +/- 0.0496` |
| RL 到 LR | `0.5681 +/- 0.0986` | `0.5530 +/- 0.1107` |

跨方向性能明显低于 mixed 协议，说明这是一个困难的方向迁移任务。需要注意，
单方向训练使用的有标签训练图数量也约为 mixed 协议的一半，因此该比较本身不能
将全部性能下降归因于方向域偏移。结合原始连接特征可以高精度识别 LR/RL 的诊断
结果，方向偏移仍是后续工作的明确动机，但当前简单方向对抗方法不足以稳定解决
该问题。下一步更值得测试配对一致性损失、显式方向校正或更严格的域泛化方法。

## 下一条模型主线：LR/RL 配对一致性 BrainGNN

执行日期：2026-06-13。

当前方向对抗分支位于最终图嵌入之后，而强基线最终嵌入的方向线性探针 balanced
accuracy 已只有 `0.5884`。因此，继续增强最终嵌入上的方向对抗约束可利用的方向
信号有限，并可能同时删除任务判别信息。下一条主线改为直接利用同一受试者、同一
任务的 LR/RL 真配对：

```text
L = L_task + L_unit + 0.1 * L_GLC + lambda_pair * JS(p_LR, p_RL)
```

实现原则：

- 全部训练图继续参与任务分类，未配对图不会被丢弃；
- 仅完整 LR/RL 对计算预测 JS 散度；
- 每个 epoch 将训练配对随机划分到普通训练 step 中，每个配对使用一次；
- 配对前向临时关闭 Dropout 和 BatchNorm 统计更新，但保留梯度，避免将随机预测
  噪声误当作方向差异；
- 普通任务批次和配对批次保持受试者划分隔离，不使用验证或测试配对训练模型。

专用实验入口为 `18-run_hcp_pair_consistency.py`。与前一轮三个种子同时改变划分和
初始化不同，该实验固定 `split_seed=123` 的五折划分，只改变模型初始化种子
`{123, 456, 789}`，从而能够单独评估训练随机性。

默认筛选：

```text
lambda_pair = {0.05, 0.1, 0.2, 0.5}
```

权重使用两个初始化种子的验证集结果选择。候选模型首先必须满足验证 balanced
accuracy 相对基线下降不超过 `0.5` 个百分点，然后在合格候选中选择验证集 LR/RL
配对预测一致率最高者。最终模型与固定划分基线进行 `5 folds x 3 initialization
seeds` 比较。

端到端两 epoch 冒烟实验已经通过，确认完整配对数可正确进入各划分：示例 fold 0
包含训练 `181` 对、验证 `53` 对和测试 `57` 对。冒烟结果仅用于验证代码路径，
不能用于判断方法有效性。

为校准正式权重范围，另外完成了固定 fold 0、初始化种子 123 的 10-epoch 串行
探针。`lambda_pair=0.1` 的验证 balanced accuracy 和配对一致率均高于同周期
基线；`lambda_pair=1` 已明显降低任务性能，`lambda_pair=5` 出现严重退化。
因此正式筛选保留集中在较小范围的 `{0.05, 0.1, 0.2, 0.5}`。该短周期单折结果
只用于确定搜索范围，不能作为模型有效性证据。

配对模型每个训练 step 需要一次额外前向。单 GPU 上同时运行三个 fold 会产生严重
资源争用，因此完整实验建议使用 `--jobs 1` 串行执行。

### 配对主线训练与评估增强

为降低短周期探针中观察到的训练后期波动，正式协议进一步加入以下约束：

- 基线和配对模型使用相同的多目标检查点规则：保留验证 balanced accuracy 距离
  训练过程最优值不超过 `0.5` 个百分点的 epoch，再依次最大化验证配对一致率、
  最小化验证配对预测 JS；
- 至少训练 40 epoch，主验证指标连续 20 epoch 未改善时提前停止，并记录最优主
  指标 epoch、最终选择 epoch 和实际训练 epoch 数；
- 使用配对任务频数的逆平方根进行损失平衡，降低 EMOTION 等配对较多任务的主导
  作用，同时避免 MOTOR 的 13 个配对被完全逆频率权重过度放大；
- epoch 0-9 不施加配对损失，epoch 10-29 将配对权重线性增加到目标值；
- 补充每任务配对一致率、任务宏平均配对一致率、配对预测 JS、同时正确率、
  LR/RL 平均概率集成指标，以及受试者级 bootstrap 95% 区间。

增强版 8-epoch 端到端冒烟实验已通过。该短实验中配对一致率和配对集成 balanced
accuracy 有所提高，但普通 balanced accuracy 下降，进一步说明正式结论必须使用
完整固定划分实验和预设的非劣检查点、权重选择规则。

### 配对一致性正式实验结果

执行日期：2026-06-13。完整结果位于：

`experiments/hcp900_pair_consistency_20260613/`

正式实验完成了 60 个 fold 训练任务，包含：

- 固定同一受试者五折划分的强基线 `5 folds x 3 initialization seeds`；
- `lambda_pair = {0.05, 0.1, 0.2, 0.5}` 在两个初始化种子上的完整筛选；
- 验证集选择的最终权重在第三个初始化种子上的补充评估。

根据预设的验证 balanced accuracy 非劣约束与验证配对一致率规则，最终选择
`lambda_pair=0.2`。

| 模型 | Balanced accuracy | Macro F1 | 配对一致率 | 任务宏平均配对一致率 |
|---|---:|---:|---:|---:|
| 固定划分强基线 | `0.7938 +/- 0.0556` | `0.7978 +/- 0.0547` | `0.7418 +/- 0.0572` | `0.7030 +/- 0.0733` |
| 配对一致性 BrainGNN | `0.8058 +/- 0.0505` | `0.8091 +/- 0.0509` | `0.7506 +/- 0.0582` | `0.7159 +/- 0.0813` |

15 个匹配 fold 的探索性比较显示：

- balanced accuracy 平均提高 `0.0119`，15 个 fold 中 13 个提高，
  配对 t 检验 `p=0.03136`；
- macro F1 平均提高 `0.0114`，`p=0.03119`；
- 配对一致率平均提高 `0.0088`，`p=0.34127`；
- 任务宏平均配对一致率提高 `0.0129`，`p=0.35737`；
- 配对预测 JS 降低 `0.0034`，`p=0.13203`；
- pool1 ROI 选择 Jaccard 提高 `0.0041`，`p=0.08373`。

由于交叉验证 fold 共享部分训练受试者，上述 p 值仅作为探索性统计。当前最稳妥的
论文结论是：配对一致性训练在固定数据划分、多个初始化下提高了任务解码性能，并
带来轻微但未达到显著水平的方向一致性改善。不能将结果表述为已经实现强 LR/RL
方向不变性。

任务级改善并不均匀。GAMBLING 配对一致率提高约 `0.0474`，MOTOR 提高约
`0.0889`，EMOTION 提高约 `0.0257`；RELATIONAL、WM、LANGUAGE 和 SOCIAL
的一致率没有改善。该异质性支持在论文中报告任务级结果，并将配对一致性解释为
对困难任务更有帮助的正则化，而不是对所有任务统一有效的域不变机制。
